import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1ABC9C', // Define your primary color here
    },
    // Define other colors if needed
  },
  // Add additional theme configurations here
});

export default theme;
